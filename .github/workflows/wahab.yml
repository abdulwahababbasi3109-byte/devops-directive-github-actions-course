
name: Wahab Workflow
on: 
  push:
    branches:
      - staging 
env: 
  APP_ENV: "dev"
jobs: 
  build: 
    runs-on: ubuntu-24.04
    steps:
      - name: Outputting the code
        id: Step1
        run: echo "version=1.0.0" >> $GITHUB_OUTPUT
      - name: Step2 getting the code
        run:  echo "Ouptt is ${{steps.Step1.outputs.version}}"