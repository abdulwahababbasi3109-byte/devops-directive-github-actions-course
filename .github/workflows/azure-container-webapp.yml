name: Hello World 
on: workflow_dispatch
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:   
      - name: build
        run: echo "A demo build"
      
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test
        run: echo "A demo test"

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - run: echo "Wha tis this dpeoly"
      
